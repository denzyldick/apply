box: duythien/phalcon@1.2.6
build:
  steps:
    -  script:
        name: install phalcon php
        code: |-
          sudo apt-add-repository ppa:phalcon/ -y
          sudo apt-get update -y
          sudo apt-get install -y php5-phalcon