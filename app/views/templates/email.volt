<!DOCTYPE html>
<html>
<meta charset="utf-8">
<head>

</head>
<body>
<div><img src="{{ url("img/logo.png") }}" width="20" height="20"/>&nbsp;{{ config.company.name }}</div>
{% block content %}{% endblock %}</body>
</html>